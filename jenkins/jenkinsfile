pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {

                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                   // Add -u 0:0 to run as root
                   args '-u 0:0 -v jenkins-docker-certs:/certs/client:ro'
                }

            }
            steps {
                dir('bugtracker-backend'){
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
